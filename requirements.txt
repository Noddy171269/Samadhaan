# Calendar-accurate month/day arithmetic for MSMED statutory-interest accrual
# (engine.statutory_interest). Chosen over a hand-rolled counter because
# relativedelta clamps end-of-month boundaries correctly and is defensible.
python-dateutil>=2.9

# Optional — only the LLM classifier (classifier.py) uses this. Without it (or
# without ANTHROPIC_API_KEY) the classifier cleanly falls back and the batch still
# runs end to end; every other stage works with no third-party deps.
anthropic>=0.40
